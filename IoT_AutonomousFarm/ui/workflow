 
 
 
 
 
 
 
 
                  [ WebApp (UI) ] ──>  [ Catalog API ]  ──>  [ PostgreSQL DB ]
                           │                │
                           │                ├──> DataAnalysis (requests stats)
                           │                ├──> TimeShift (scheduled events)
                           │                ├──> Management Services (thresholds, sensors, chat_id)
                           │                └──> Telegram Bot (OTP, chat_id linking)
